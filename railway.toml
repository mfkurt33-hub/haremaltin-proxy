[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[nixpacks]
pkgs = ["chromium", "nss", "freetype", "harfbuzz", "ca-certificates", "ttf-freefont"]
